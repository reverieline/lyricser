services:
  lyricser:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=development
      - BASE_PATH=/lyricser
      - ADMIN_USERNAME=sc
      - ADMIN_PASSWORD=sc123698745